#!/usr/bin/env bash

wallpapers_path="$HOME/Pictures/wallpaper"

while true; do
  pid = pidof swaybg
  echo $pid
  swaybg -i $(find $wallpapers_path -type f | shuf -n 1) -m fill
  sleep 29m
done
sleep 1m
